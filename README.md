Przełącznik do zmiany true/false - switch = > onCheckedChanged()
editTekst - pole do wypełnienia - .addTextChangedListener() = > TextWatcher()
.isEmpty - sprawdza czy to jest puste (null)
konwertowanie na int => Integer.parseInt();
layout.setBackgroundColor(Color.parseColor("#2196F3")); - zmiana koloru
